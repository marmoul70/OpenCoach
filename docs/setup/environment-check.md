# OpenCoach — Environment Check

## Purpose

The `check-environment.sh` script verifies that the host system
provides the minimum environment required by OpenCoach.

## Usage

From the project root:

```bash
./scripts/bootstrap/check-environment.sh